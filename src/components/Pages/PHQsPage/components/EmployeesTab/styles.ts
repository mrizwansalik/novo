import { Col, Row } from "reactstrap";
import styled from "styled-components";
export const Container = styled.div`
  color: #475259;
  font-weight: 500;
`;

export const WorkerContainer = styled(Row)``;

export const StyledCol = styled(Col)`
  padding: 12px;
`;
