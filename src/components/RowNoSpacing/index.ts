import { Row } from "reactstrap";
import styled from "styled-components";

const RowNoSpacing = styled(Row)`
  margin-left: 0px;
  margin-right: 0px;
  padding-left: 0px;
  padding-right: 0px;
`;

export default RowNoSpacing;
