export enum PROSPECT_TYPE {
  ACTIVE = "active",
  INACTIVE = "inactive",
  WON = "won",
  LOST = "lost",
}

export enum PROSPECT_SORT_BY {
  NAME = "name",
  CREATED = "created",
  EFFECTIVE_DATE = "effectiveDate",
  PRIMARY_ADVISOR = "primaryAdvisor",
}

export enum PROSPECT_SORT_TYPE {
  ASD = 1,
  DESC = -1,
}

export enum GENDER {
  MALE = "Male",
  FEMALE = "Female",
}
