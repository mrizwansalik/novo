export enum DocumentStatus {
  UN_SUBMITTED = "unsubmitted",
  SUBMITTED = "submitted",
  ACCEPTED = "accepted",
  REJECTED = "rejected",
}

export enum SignatureState {
  FULLY_SIGNED = "fully_signed",
  NEEDS_SIGNATURES = "needs_signatures",
}

export enum DocumentSubmitStatus {
  PENDING = "pending",
  ACCEPTED = "accepted",
  REJECTED = "rejected",
}
