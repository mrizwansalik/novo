export enum FileAccessType {
  PRIVATE = "private",
  PUBLIC = "public",
}

export enum BaseFilePath {
  PRIVATE = "private/docs",
  PUBLIC = "media/profile_pictures/",
}

export enum ContentType {
  CSV = "text/csv",
  XLSX = "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
}
